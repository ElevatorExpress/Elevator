package util.states;

import scheduler.Scheduler;

/**
 * Represents the SchedulerWorking State
 */
public class SchedulerEmergency extends SchedulerState{

    protected SchedulerEmergency(Scheduler context) {
        super(context);
    }
}
